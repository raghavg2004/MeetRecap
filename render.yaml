services:
  - type: web
    name: meetrecap
    runtime: node
    plan: free
    buildCommand: npm ci --prefer-offline --no-audit --legacy-peer-deps
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
